os: osx

install:
  - brew update
  - brew install hugo

script:
  - hugo

deploy:
  local_dir: public
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  keep_history: true
  on:
    branch: master